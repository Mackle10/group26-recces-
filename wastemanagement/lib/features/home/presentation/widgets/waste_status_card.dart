import 'package:flutter/material.dart';

class WasteStatusCard extends StatelessWidget {
  const WasteStatusCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text('Waste Stats'),
    );
  }
}
